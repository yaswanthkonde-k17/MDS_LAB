import time\nimport numpy as np\n\n# Define the size of the lists/arrays\nsize = 1000000\n\n# Create two large Python lists\nlist_a = list(range(size))\nlist_b = list(range(size))\n\n# Create two large NumPy arrays\narray_a = np.arange(size)\narray_b = np.arange(size)\n\n# Measure time for list addition\nstart_time = time.time()\nresult_list = [a + b for a, b in zip(list_a, list_b)]\nlist_time = time.time() - start_time\n\n# Measure time for array addition\nstart_time = time.time()\nresult_array = array_a + array_b\narray_time = time.time() - start_time\n\nprint(f"Time taken for list addition: {list_time:.6f} seconds")\nprint(f"Time taken for array addition: {array_time:.6f} seconds")\nprint(f"NumPy array addition is {list_time / array_time:.2f} times faster than list addition.")
